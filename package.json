{
  "name": "motoristaops",
  "version": "0.3.1",
  "private": true,
  "type": "module",
  "description": "MotoristaOps: plataforma operacional e financeira para motoristas, alimentada pela base MotoristaOps.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync:data": "node scripts/sync-data.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "quality": "npm run sync:data && npm run validate:data && npm run build",
    "security": "npm audit --omit=dev"
  },
  "dependencies": {
    "astro": "^7.1.6",
    "exceljs": "^4.4.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  },
  "overrides": {
    "uuid": "11.1.1"
  }
}
