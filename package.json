{
  "name": "dash-drive",
  "version": "0.3.0",
  "private": true,
  "type": "module",
  "description": "Dashboard operacional e financeiro para motoristas, alimentado pela base MotoristaOPS.",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "sync:data": "node scripts/sync-data.mjs",
    "validate:data": "node scripts/validate-data.mjs",
    "quality": "npm run validate:data && npm run build"
  },
  "dependencies": {
    "astro": "^5.0.0",
    "xlsx": "^0.18.5"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20"
  }
}
